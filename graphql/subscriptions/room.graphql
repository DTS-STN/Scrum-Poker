subscription RoomSubscription($room: ID!) {
  roomUpdated(room: $room) {
    id
    host {
        id
        name
    }
    users {
        id
    }
    isShown
    timer {
        timestamp
        duration
    }
    cards 
  }
}