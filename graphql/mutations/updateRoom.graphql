mutation updateRoom($updateRoomId: ID!, $updateRoomUsers: [ID], $isShown: Boolean!, $cards: [Int]) {
  updateRoom(id: $updateRoomId, users: $updateRoomUsers, isShown: $isShown, cards: $cards) {
    success
    message
  }
}